<h1>Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? or (article.status == "Private" and not admin_signed_in?) %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>